# Reporters

Reference for Warden's output formats.

---

## 1. Reporter Modes

There are three reporters. Each one receives the same events and decides what to render.

| Mode | Purpose | Channel | Implementation |
|------|---------|---------|----------------|
| **TTY** | The interactive experience. Boxes, colors, spinners, inline code snippets. Designed to make findings easy to scan and act on. | stderr (progress), stdout (findings report) | `Reporter` class + Ink (`ink-runner.tsx`) |
| **Plain** | CI logs and piped output. Every line is timestamped and prefixed so you can grep, tail, and correlate with other CI output. Full findings report still goes to stdout. | stderr (timestamped progress), stdout (findings report) | `Reporter` (plain path), `renderSkillCI()` in `terminal.ts` |
| **JSONL** | Machine-readable run log. One JSON object per line for downstream tooling: dashboards, cost tracking, trend analysis, alerting. | File (`--output` or auto `~/.local/warden/runs/`) | `writeJsonlReport()` in `jsonl.ts` |

**Plain format**: `[{ISO-8601}] warden: {message}` on stderr. Generated by `timestamp()` in `tty.ts`.

**GitHub Actions** runs in Plain mode. The Action workflow (`pr-workflow.ts`) adds GitHub-specific side effects on top: `::group::` blocks, `::warning::` annotations, `GITHUB_OUTPUT` variables, and GitHub API calls for PR reviews, check runs, and comment management. These are workflow orchestration, not a separate reporter. See [Section 8: Action Workflow Logging](#8-action-workflow-logging).

### Verbosity Levels

| Level | Flag | Effect |
|-------|------|--------|
| Quiet | `-q` | Final findings report only (stdout). No progress, no summary. |
| Normal | (default) | Skill/file/hunk progress + findings report + summary |
| Verbose | `-v` | + real-time per-finding lines during skill execution |
| Debug | `-vv` | + token counts, prompt sizes, extraction methods, suggested fix descriptions |

---

## 2. Per-Event Reference

Events are numbered from the catalog below. Each event shows its output in every reporter. `--` means the event is intentionally absent in that reporter.

### CLI Lifecycle

#### 1. `header`

Fields: `version`

**TTY:**
```
 __    __              _
/ / /\ \ \__ _ _ __ __| | ___ _ __
\ \/  \/ / _` | '__/ _` |/ _ \ '_ \
 \  /\  / (_| | | | (_| |  __/ | | |
  \/  \/\__,_|_|  \__,_|\___|_| |_|
v0.4.0
```
(All lines dimmed)

**Plain:**
```
[2026-02-08T14:30:44.000Z] warden: Warden v0.4.0
```

**JSONL:** --

#### 2. `context_start`

Fields: `description` (e.g., "Analyzing changes from HEAD~3...")

**TTY:** Dimmed description line

**Plain:**
```
[2026-02-08T14:30:44.100Z] warden: Analyzing changes from HEAD~3...
```

**JSONL:** --

#### 3. `context_files`

Fields: `files[]` with status and chunks

**TTY:**
```
FILES  5 files · 10 chunks
  + src/cli/main.ts (2 chunks)
  ~ src/types/index.ts (3 chunks)
  - src/old-file.ts (1 chunk)
  ... and 2 more
```
Status symbols: `+` (green, added), `~` (yellow, modified), `-` (red, removed). Max 10 files shown.

**Plain:**
```
[2026-02-08T14:30:44.200Z] warden: Found 5 changed files with 10 chunks
[2026-02-08T14:30:44.200Z] warden:   + src/cli/main.ts (2 chunks)
[2026-02-08T14:30:44.200Z] warden:   ~ src/types/index.ts (3 chunks)
[2026-02-08T14:30:44.200Z] warden:   - src/old-file.ts (1 chunk)
[2026-02-08T14:30:44.200Z] warden:   ... and 2 more
```

**JSONL:** --

#### 4. `config_triggers`

Fields: `loaded` (total count), `matched` (count), `triggers[]`

**TTY:**
```
CONFIG  6 triggers · 4 matched
  checkmark security-check (security-lint)
  checkmark style-guide (style)
```

**Plain:**
```
[2026-02-08T14:30:44.300Z] warden: Config: 6 triggers, 4 matched
[2026-02-08T14:30:44.300Z] warden:   security-check (security-lint)
[2026-02-08T14:30:44.300Z] warden:   style-guide (style)
```

**JSONL:** --

---

### Skill Execution

#### 5. `skill_start`

Fields: `name`, `displayName`, `fileCount`

**TTY:** Spinner icon + skill name + `[done/total files]` + finding count + running cost

**Plain:**
```
[2026-02-08T14:30:45.123Z] warden: Running security-review (3 file(s))...
```

**JSONL:** --

#### 6. `file_complete`

Fields: `filename`, `durationMs`, `findings[]`, `usage?`

**TTY:** File disappears from dynamic area (only running files shown). Checkmark + filename + severity dots + duration + cost appear in the post-unmount summary.

**Plain:**
```
[2026-02-08T14:30:46.500Z] warden:   security-review > src/api/auth.ts done 1.8s $0.01 1 finding(s)
```
Cost omitted when no usage data. Finding count omitted when zero.

**JSONL:** Captured in `JsonlRecord.files[]` array

#### 7. `hunk_start`

Fields: `filename`, `hunkNum`, `totalHunks`, `lineRange`

**TTY:** -- (progress shown via file-level spinner)

**Plain:**
```
[2026-02-08T14:30:45.200Z] warden:   security-review > src/api/auth.ts [1/3] L10-45
```

**JSONL:** --

#### 8. `skill_complete`

Fields: `name`, `durationMs`, `findingCount`, `usage?`

**TTY:** Skill moves from spinner to checkmark in the dynamic area: `checkmark security-review [4.2s]` (green checkmark, duration dimmed). After all skills finish and Ink unmounts, the full per-skill + per-file breakdown prints to stderr.

**Plain:**
```
[2026-02-08T14:30:48.600Z] warden: security-review completed in 4.2s - 2 findings (1 high, 1 medium)
```

At `Verbose+`, per-finding lines follow:
```
[2026-02-08T14:30:48.601Z] warden:   [high] src/api/auth.ts:42: SQL injection risk
[2026-02-08T14:30:48.601Z] warden:   [medium] src/utils/helpers.ts:15: Unsafe type assertion
```

At `Debug`, suggested fix descriptions also shown.

**JSONL:** Full `JsonlRecord` with findings, files, usage, auxiliaryUsage

#### 9. `skill_skipped`

Fields: `name`

**TTY:** `skip-icon {displayName} [skipped]`

**Plain:**
```
[2026-02-08T14:30:48.600Z] warden: security-review skipped
```

**JSONL:** --

#### 10. `skill_error`

Fields: `name`, `error`

**TTY:** `cross-icon {displayName} - {error}`

**Plain:**
```
[2026-02-08T14:30:48.600Z] warden: ERROR: security-review - API timeout after 30s
```

**JSONL:** --

---

### Findings

#### 11. `finding`

Fields: `id`, `severity`, `title`, `location?`, `description`, `suggestedFix?`, `confidence?`

Findings appear in two places: real-time during skill execution (stderr, verbosity-gated, Plain only) and in the final findings report (stdout, always shown).

**Real-time (stderr, `Verbose+`, Plain only):**

```
[2026-02-08T14:30:48.601Z] warden:   [high] src/api/auth.ts:42: SQL injection risk
```

TTY does not emit per-finding lines during execution. The Ink UI shows severity dots per-file in the progress display instead.

**Final report (stdout, always):** See "Findings Report" section below.

**JSONL:** Captured in `JsonlRecord.findings[]` array

#### 12. `finding_summary`

Fields: `bySeverity` counts

**TTY:** Colored breakdown with severity dots

**Plain:** Embedded in `skill_complete` line as `{n} findings ({breakdown})`

**JSONL:** In summary record `bySeverity` field

---

### Findings Report (stdout)

After all skills complete, findings are rendered to stdout. This is separate from the progress events above (which go to stderr).

**TTY:** Box-rendered findings per skill via `renderSkillBoxTTY()`:
```
┌── security-review ────────────────────────────── [4.2s] ──┐
│ 2 findings: . 1 high  . 1 medium                          │
├───────────────────────────────────────────────────────────┤
│                                                           │
│ . (high) SQL injection risk in query builder              │
│   src/api/auth.ts:42  +0.8s                               │
│   42 | const query = buildQuery(userId);                   │
│                                                           │
│   User input is interpolated directly into a SQL query... │
│                                                           │
│   Suggested fix:                                          │
│   - const query = buildQuery(userId);                     │
│   + const query = buildQuery(sanitize(userId));           │
│                                                           │
├───────────────────────────────────────────────────────────┤
│                                                           │
│ . (medium) Unsafe type assertion on external input        │
│   src/utils/helpers.ts:15  +1.2s                          │
│   15 | const data = response as InternalType;              │
│                                                           │
│   External API response is cast without validation.       │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

Each finding shows: severity badge, title, location with elapsed time, source code line (read from disk), description, and suggested fix diff (colored: green for `+`, red for `-`, cyan for `@@`).

**Plain:** Compact text per skill via `renderSkillCI()`:
```
security-review (4.2s) - 2 findings (1 high, 1 medium)
  WARN: 1 chunk failed to analyze
  [high] src/api/auth.ts:42 - SQL injection risk in query builder (+0.8s)
  confidence: high
  User input is interpolated directly into a SQL query string without parameterization.

  Suggested fix:
  - const query = buildQuery(userId);
  + const query = buildQuery(sanitize(userId));

  [medium] src/utils/helpers.ts:15 - Unsafe type assertion on external input (+1.2s)
  confidence: medium
  External API response is cast to an internal type without validation.
```

Per-skill warnings (`failedHunks`, `failedExtractions`) are shown inline.

**JSONL:** Full structured records (see Section 3)

---

### Debug

#### 13. `large_prompt`

Fields: `filename`, `lineRange`, `chars`, `estimatedTokens`

**TTY:** `warning-icon Large prompt for {filename}:{lineRange}: {n}k chars (~{n}k tokens)`

**Plain:**
```
[2026-02-08T14:30:45.300Z] warden: WARN: Large prompt for src/api/auth.ts:L10-200: 32k chars (~8k tokens)
```

**JSONL:** --

#### 14. `prompt_size`

Fields: `filename`, `lineRange`, `systemChars`, `userChars`, `totalChars`, `estimatedTokens`

At `Debug` only.

**TTY:** `[debug] Prompt for {filename}:{lineRange}: system={n}, user={n}, total={n} chars (~{n} tokens)`

**Plain:**
```
[2026-02-08T14:30:45.300Z] warden: DEBUG: Prompt for src/api/auth.ts:L10-45: system=1200, user=3400, total=4600 chars (~1150 tokens)
```

**JSONL:** --

#### 15. `extraction_result`

Fields: `filename`, `lineRange`, `findingsCount`, `method`

At `Debug` only.

**TTY:** `[debug] Extracted {n} finding(s) from {filename}:{lineRange} via {method}`

**Plain:**
```
[2026-02-08T14:30:45.400Z] warden: DEBUG: Extracted 1 finding(s) from src/api/auth.ts:L10-45 via json
```

**JSONL:** --

---

### Summary

#### 16. `analysis_complete`

Fields: `findingsCount`, `failedHunks?`, `failedExtractions?`, `skippedFiles?`, `usage?`, `totalDuration`

This is a multi-line summary section, not a single event.

**TTY:**
```
SUMMARY
2 findings: . 1 high  . 1 medium
⚠  2 chunks failed to analyze
⚠  1 finding extraction failed
3 files skipped
Analysis completed in 4.2s · 5.0k in / 800 out · $0.01
```
(Counts colored by severity. Warnings in yellow. Usage line dimmed.)

**Plain:**
```
[2026-02-08T14:30:56.000Z] warden: Summary: 2 findings (1 high, 1 medium)
[2026-02-08T14:30:56.000Z] warden: WARN: 2 chunks failed to analyze
[2026-02-08T14:30:56.000Z] warden: WARN: 1 finding extraction failed
[2026-02-08T14:30:56.000Z] warden: 3 files skipped
[2026-02-08T14:30:56.000Z] warden: Usage: 5.0k input, 800 output, $0.01
[2026-02-08T14:30:56.000Z] warden: Total time: 4.2s
```
Warnings and skipped files only shown when non-zero.

**JSONL:** Summary record (the last line in the JSONL file)

---

## 3. JSONL Specification

Formal schema: [`specs/jsonl-schema.json`](./jsonl-schema.json)

Example payloads: [`specs/jsonl-examples.jsonl`](./jsonl-examples.jsonl)

### Record Types

Each line in a JSONL file is one of three record types, discriminated by the presence of `type` and `skill` fields:

| Record | Discriminator | When |
|--------|--------------|------|
| **Skill** | Has `skill` field, no `type` field | One per skill that ran |
| **Summary** | `"type": "summary"` | Always the last line |
| **Fix Evaluation** | `"type": "fix-evaluation"` | After fix evaluation completes (not yet wired) |

### Skill Record

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `run` | `RunMetadata` | yes | Timestamp, duration, cwd |
| `skill` | `string` | yes | Skill name |
| `summary` | `string` | yes | Human-readable summary |
| `findings` | `Finding[]` | yes | All findings (may be empty) |
| `metadata` | `object` | no | Arbitrary skill metadata |
| `durationMs` | `number` | no | Skill execution time |
| `usage` | `UsageStats` | no | Token usage and cost |
| `auxiliaryUsage` | `Record<string, UsageStats>` | no | Auxiliary LLM usage (extraction, dedup). Present only when auxiliary calls occurred. |
| `files` | `FileRecord[]` | no | Per-file breakdown |

### Summary Record

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `run` | `RunMetadata` | yes | Timestamp, duration, cwd |
| `type` | `"summary"` | yes | Discriminator |
| `totalFindings` | `integer` | yes | Total across all skills |
| `bySeverity` | `BySeverity` | yes | Breakdown by severity level |
| `usage` | `UsageStats` | no | Aggregate usage across skills |
| `auxiliaryUsage` | `Record<string, UsageStats>` | no | Merged auxiliary usage. Present only when auxiliary calls occurred. |

### Fix Evaluation Record

**Status: defined, not wired.** The `JsonlFixEvaluationRecord` interface exists in `src/cli/output/jsonl.ts` but `writeJsonlReport()` does not produce it yet.

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `run` | `RunMetadata` | yes | Timestamp, duration, cwd |
| `type` | `"fix-evaluation"` | yes | Discriminator |
| `evaluated` | `integer` | yes | Comments sent to LLM |
| `resolved` | `integer` | yes | Fixes confirmed |
| `needsAttention` | `integer` | yes | Fix attempts that failed |
| `skipped` | `integer` | yes | Not evaluated |
| `failedEvaluations` | `integer` | yes | Evaluation errors |
| `usage` | `UsageStats` | no | Aggregate evaluation usage |
| `evaluations` | `FixEvalDetail[]` | no | Per-comment details |

### Shared Types

**RunMetadata**: `{ timestamp: string, durationMs: number, cwd: string }`

**UsageStats**: `{ inputTokens: int, outputTokens: int, cacheReadInputTokens?: int, cacheCreationInputTokens?: int, costUSD: number }` -- `inputTokens` is the total input token count; `cacheReadInputTokens` and `cacheCreationInputTokens` are subsets of it.

**Finding**: `{ id: string, severity: Severity, confidence?: Confidence, title: string, description: string, location?: Location, suggestedFix?: SuggestedFix, elapsedMs?: number }`

**Location**: `{ path: string, startLine: int, endLine?: int }`

**SuggestedFix**: `{ description: string, diff: string }`

**FileRecord**: `{ filename: string, findings: int, durationMs?: number, usage?: UsageStats }`

**BySeverity**: `{ critical: int, high: int, medium: int, low: int, info: int }`

**FixEvalDetail**: `{ path: string, line: int, findingId?: string, verdict: "not_attempted" | "attempted_failed" | "resolved" | "re_detected", reasoning?: string, durationMs: number, usage: UsageStats }`

### Output Location

- Explicit: `--output path/to/file.jsonl`
- Automatic: `~/.local/warden/runs/{dirname}_{timestamp}.jsonl` (override with `WARDEN_STATE_DIR`)

---

## 4. `--json` Output

`renderJsonReport()` in `src/cli/terminal.ts` outputs a single JSON blob to stdout. Distinct from JSONL.

```json
{
  "reports": [
    {
      "skill": "security-review",
      "summary": "...",
      "findings": [...],
      "metadata": {...},
      "durationMs": 3500,
      "usage": {...}
    }
  ],
  "summary": {
    "totalFindings": 2,
    "bySeverity": { "critical": 0, "high": 1, "medium": 1, "low": 0, "info": 0 },
    "usage": {...}
  }
}
```

The `reports` array mirrors `SkillReport` but excludes `files`, `skippedFiles`, `failedHunks`, `failedExtractions`, and `auxiliaryUsage`. The `summary.usage` is an aggregate across all skills.

---

## 5. Formatting Functions

All reporters use shared formatters from `src/cli/output/formatters.ts`.

| Function | Input | Output | Example |
|----------|-------|--------|---------|
| `formatCost(costUSD)` | `number` | `$X.XX` (always 2 decimals) | `$0.09`, `$1.50`, `$0.00` |
| `formatTokens(tokens)` | `number` | Compact count | `850`, `1.2k`, `1.5M` |
| `formatDuration(ms)` | `number` | Human duration | `250ms`, `1.5s`, `12.3s` |
| `formatElapsed(ms)` | `number` | Elapsed prefix | `+250ms`, `+1.5s` |
| `formatLocation(path, start?, end?)` | `string, number?, number?` | File location | `src/a.ts:10`, `src/a.ts:10-20` |
| `formatUsage(usage)` | `UsageStats` | TTY usage | `2.5k in / 450 out . $0.09` |
| `formatUsagePlain(usage)` | `UsageStats` | Plain usage | `2.5k input, 450 output, $0.09` |
| `formatStatsCompact(ms?, usage?, aux?)` | Mixed | One-line stats | `T 1.5s . 2.5k in / 450 out . $0.09` |
| `formatAuxiliarySuffix(aux)` | `AuxiliaryUsageMap` | Aux breakdown | `(+extraction: $0.01, +dedup: $0.00)` |
| `formatFindingCounts(counts)` | `Record<Severity, number>` | TTY colored | `2 findings: . 1 high  . 1 medium` |
| `formatFindingCountsPlain(counts)` | `Record<Severity, number>` | Plain text | `2 findings (1 high, 1 medium)` |
| `formatSeverityBadge(severity)` | `Severity` | Colored dot + text | `. (high)` |
| `formatSeverityPlain(severity)` | `Severity` | Bracketed | `[high]` |
| `countBySeverity(findings)` | `Finding[]` | `Record<Severity, number>` | `{ critical: 0, high: 1, ... }` |
| `pluralize(count, singular, plural?)` | `number, string` | Pluralized word | `file` / `files` |

---

## 6. Master Checklist

Matrix of every reporter event. Legend:
- `[x]` implemented
- `[-]` intentionally absent (footnote explains why)
- `[ ]` gap/TODO

### CLI Lifecycle

| # | Event | TTY | Plain | JSONL |
|---|-------|-----|-------|-------|
| 1 | `header` | [x] | [x] | [-]^1 |
| 2 | `context_start` | [x] | [x] | [-]^1 |
| 3 | `context_files` | [x] | [x] | [-]^1 |
| 4 | `config_triggers` | [x] | [x] | [-]^1 |

### Skill Execution

| # | Event | TTY | Plain | JSONL |
|---|-------|-----|-------|-------|
| 5 | `skill_start` | [x] | [x] | [-]^1 |
| 6 | `file_complete` | [x] | [x] | [x] |
| 7 | `hunk_start` | [-]^2 | [x] | [-]^1 |
| 8 | `skill_complete` | [x] | [x] | [x] |
| 9 | `skill_skipped` | [x] | [x] | [-]^1 |
| 10 | `skill_error` | [x] | [x] | [-]^1 |

### Findings

| # | Event | TTY | Plain | JSONL |
|---|-------|-----|-------|-------|
| 11 | `finding` | [x] | [x] | [x] |
| 12 | `finding_summary` | [x] | [x] | [x] |

### Debug

| # | Event | TTY | Plain | JSONL |
|---|-------|-----|-------|-------|
| 13 | `large_prompt` | [x] | [x] | [-]^1 |
| 14 | `prompt_size` | [x] | [x] | [-]^1 |
| 15 | `extraction_result` | [x] | [x] | [-]^1 |

### Summary

| # | Event | TTY | Plain | JSONL |
|---|-------|-----|-------|-------|
| 16 | `analysis_complete` | [x] | [x] | [x] |

### Footnotes

1. JSONL captures skill results and summary as structured records. Transient progress events are not applicable.
2. TTY shows only running files in the dynamic Ink area; individual hunk starts are not shown. Completed file details appear in the post-unmount summary.

---

## 7. Gaps

### JSONL: Missing Diagnostic Fields

`SkillReport` has three diagnostic fields that `writeJsonlReport()` silently drops:

| Field | On `SkillReport` | In JSONL | Impact |
|-------|-----------------|----------|--------|
| `skippedFiles` | `SkippedFile[]` | not written | Operators can't see which files were skipped and why |
| `failedHunks` | `number` | not written | No structured tracking of analysis failures |
| `failedExtractions` | `number` | not written | No structured tracking of extraction errors |

These are shown in TTY/Plain summary warnings but lost in the JSONL record. Since JSONL is the format for downstream tooling, these should be added to enable alerting on degraded runs.

### JSONL: Missing Event Types

| Event | Status | Notes |
|-------|--------|-------|
| Fix evaluation (per-comment) | Type defined | `JsonlFixEvalDetail` in `jsonl.ts`, not wired to `writeJsonlReport()` |
| Fix evaluation (summary) | Type defined | `JsonlFixEvaluationRecord` in `jsonl.ts`, not wired to `writeJsonlReport()` |
| Dedup results | Not started | No structured capture for dedup actions |
| Stale resolved | Not started | Not captured in JSONL output |

---

## 8. Action Workflow Logging

The GitHub Action workflow (`pr-workflow.ts`) runs the same Plain reporter for skill execution, then performs additional workflow steps that produce their own logging. These are not reporter events. They use `logAction()` and `warnAction()` from `tty.ts`, which output in Plain format (`[ISO-8601] warden: {msg}`), plus GitHub-specific protocols.

### Context and Configuration

Inside `::group::` blocks with raw `console.log()`:

```
::group::Building event context
Event: pull_request
Workspace: /home/runner/work/repo/repo
::endgroup::

::group::Loading configuration
Config path: warden.toml
::endgroup::

::group::Matched triggers
- security-check: security-lint
- style-guide: style
::endgroup::
```

If no triggers matched: `No triggers matched for this event` and early return.

### Skill Execution

Each trigger runs inside a `::group::`:

```
::group::Running trigger: security-check (skill: security-lint)
[2026-02-08T14:30:45.123Z] warden: Running security-lint (3 file(s))...
[2026-02-08T14:30:46.500Z] warden:   security-lint > src/api/auth.ts done 1.8s $0.01 1 finding(s)
[2026-02-08T14:30:48.600Z] warden: security-lint completed in 4.2s - 2 findings (1 high, 1 medium)
Found 2 findings
::endgroup::
```

This is identical to Plain reporter output plus a bare `Found {N} findings` line from the trigger executor. The `::group::` wrapper is added by the trigger executor.

### Deduplication

```
[2026-02-08T14:30:49.000Z] warden: Found 12 existing comments for deduplication (8 Warden, 4 external)
[2026-02-08T14:30:49.100Z] warden: Found 3 duplicate findings for security-check
[2026-02-08T14:30:49.200Z] warden: Updated 2 existing Warden comments with skill attribution
[2026-02-08T14:30:49.300Z] warden: Added reactions to 1 existing external comments
```

On failures: `[...] warden: WARN: Failed to process 1 duplicate actions`

### Fix Evaluation

Inside `::group::Fix evaluation`:

```
[2026-02-08T14:30:50.000Z] warden: Fix evaluation: evaluating 5 unresolved comments
[2026-02-08T14:30:51.200Z] warden:   [1/5] WRZ-XPL src/api/handler.ts:42 → resolved (1.2s, 1.1k tok, $0.03)
[2026-02-08T14:30:52.600Z] warden:   [2/5] SEC-INJ src/db/query.ts:18 → attempted_failed (1.4s, 0.9k tok, $0.03)
[2026-02-08T14:30:52.600Z] warden:         reason: "The WHERE clause still concatenates user input via buildFilter"
[2026-02-08T14:30:53.500Z] warden: Fix evaluation: 3 resolved, 1 need attention, 1 skipped, 4.8k tok, $0.12
[2026-02-08T14:30:53.600Z] warden: Resolved 3 comments via fix evaluation
```

On eval error: `[...] warden: WARN:   [3/5] ... → eval_error (...)`

### Stale Resolution

```
[2026-02-08T14:30:54.000Z] warden: Resolved 4 stale Warden comments
```

Or on trigger failures: `[...] warden: Skipping stale comment resolution due to trigger failures`

On error: `[...] warden: WARN: Failed to resolve stale comments: {error}`

### Review Management

```
[2026-02-08T14:30:55.000Z] warden: Dismissed previous CHANGES_REQUESTED review
```

Or: `[...] warden: Skipping dismiss flow: cannot identify bot (using PAT or GITHUB_TOKEN instead of GitHub App)`

On error: `[...] warden: WARN: Failed to dismiss previous review: {error}`

On review post error: `[...] warden: WARN: Failed to post review for {triggerName}: {error}`

### Check Runs

On error: `[...] warden: WARN: Failed to create core check: {error}` or `Failed to update core check: {error}`

On success: `[...] warden: Created core check: {url}`

### Previous Review State

If a previous Warden review exists on the PR: `[...] warden: Previous Warden review state: {state}`

### Summary and Outputs

```
[2026-02-08T14:30:56.000Z] warden: Analysis complete: 5 total findings
```

Written to `GITHUB_OUTPUT` via `setOutput()`:
```
findings-count=5
critical-count=1
high-count=2
summary=Warden found 5 issues across 2 skills
```
